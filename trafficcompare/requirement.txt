python==3.9
tensorflow==2.10.0
# 如果需要独立GPU版本，可改成tensorflow-gpu==2.10.0（需要CUDA 11.2 + cuDNN 8.1，建议参考官方文档）
# conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1
numpy>=1.20,<1.24
# 有可能版本冲突，所以写在这里


